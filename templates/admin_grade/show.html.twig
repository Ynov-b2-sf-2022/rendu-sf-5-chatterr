{% extends 'base.html.twig' %}

{% block title %}Grade{% endblock %}

{% block body %}
    <h1>Grade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grade.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ grade.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_grade_index') }}">back to list</a>

    <a href="{{ path('app_admin_grade_edit', {'id': grade.id}) }}">edit</a>

    {{ include('admin_grade/_delete_form.html.twig') }}
{% endblock %}
